<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>WEB Codebreaker</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/css/style.css" type="text/css">
    <link href="/assets/image/favicon.ico" rel="icon" type="image/x-icon">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.2.0/jquery.min.js"></script>
    <script src="/assets/js/common.js"></script>
  </head>
  <body>
    <div>
      <span>Секретный код: <%= @secret_code ? @secret_code : '' %></span>
    </div>
    <div class="container startpage">
      <div class="col-xs-6 col-xs-offset-3 formgame">
        <% if @status %>
          <div><h2><%= @messages ? @messages : '' %></h2></div>
          <a class="btn-primary new-start" href='/'>New Game</a>
          <a class="btn-success new-start" href='/statistic'>View Statitic</a>
        <% else %>
          <h1><%= @name %> wellcome to Codebreaker</h1>
          <h3>Guess code with number from 1 to 6</h3>
          <form action="/processing" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <input type="text" name="input" value="" placeholder="CODE"  class="form-control">
            </div>
            <% if @error  %><div><%= @error %></div><% end %>
            <input type="submit" value="Submit" class="btn btn-primary">
            <button type="button" class = "btn btn-default btn-hint">Hint</button>
          </form>
          <div class='hint'>Hint :  <%= @hint ? @hint : '' %></div>
          <div>
            <% if @list_result %>
              <h3>Attempts  <%= @current_input ? @current_input : 0 %>/<%= @max_attempt ? @max_attempt : '' %></h3>
              <table class="table table-bordered">
                <% @list_result.each do |item|  %>
                  <tr>
                    <td class="text-center attempt">
                      <span><%=item[0]%></span>
                    </td>
                    <td class="text-center attempt">
                      <span><%=item[1]%></span>
                    </td>
                    <td class="text-center attempt">
                      <span><%=item[2]%></span>
                    </td>
                  </tr>
                <% end %>
              </table>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </body>
</html>
